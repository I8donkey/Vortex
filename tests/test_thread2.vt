import thread;

def worker() {
    print("  worker thread running");
}

print("before run");
object t = thread.run(worker);
print("after run, before join");
thread.join(t);
print("after join, done");
