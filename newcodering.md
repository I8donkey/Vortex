## 注释规范

代码中使用 `#` 作为注释符号，支持行尾注释和独立注释行：

```
# 这是一个独立注释行
int a = 10;       # 这是一个行尾注释
del a;            # 删除变量
```

注释内容会被编译器忽略，仅用于开发者阅读。

---

## 通用声明与操作

所有变量遵循以下声明和操作规则：

| 操作     | 语法                        | 说明                      |
| ------ | ------------------------- | ----------------------- |
| 声明变量   | `typename a;`             | 声明一个指定类型的变量，初始值由其默认值确定。 |
| 赋值     | `a = ...;`                | 将新值赋给已声明的变量。            |
| 声明并初始化 | `typename b = ...;`       | 声明变量的同时设置初始值。           |
| 同时声明   | `typename c, d;`          | 在一行中声明多个同类型变量。          |
| 声明常量   | `const typename e = ...;` | 声明一个常量，初始化后不可修改。        |
| 删除变量   | `del a;`                  | 删除指定的变量，释放其占用的资源。       |
| 删除多个变量 | `del b, c;`               | 同时删除多个变量。               |
| 删除所有变量 | `del *;`                  | **危险操作**：删除当前作用域内的所有变量。 |

### 字面量格式

整数字面量支持以下进制表示（所有进制数值均存储为相应的整数类型）：

| 进制   | 前缀   | 示例             | 说明               |
| ---- | ---- | -------------- | ---------------- |
| 二进制  | `0b` | `0b1010`       | 等价于十进制 10。       |
| 八进制  | `0o` | `0o12`         | 等价于十进制 10。       |
| 十六进制 | `0x` | `0xA` 或 `0xFF` | 等价于十进制 10 或 255。 |

浮点数也支持十六进制表示（符合 IEEE-754），例如 `0x1.2p3` 表示 `(1 + 2/16) * 2^3 = 9.0`。

---

## 容器通用操作语法规范

所有容器类型（`list`、`stack`、`queue`、`set`、`undset`、`dict`）遵循以下统一的语法和行为规范：

### 声明与初始化

- 空容器：`typename container;`（默认构造为空）。
- 使用初始化列表：`typename container = [elem1, elem2, ...];`（对于 `dict`，使用 `{key: value, ...}` 或 `[(key, value), ...]`）。

### 方法调用

- 所有方法通过 `.` 运算符调用：`container.method(arg1, arg2, ...);`
- 方法支持默认参数（如 `pos` 默认为尾部）。
- 部分方法返回新对象（如复制、集合运算），部分方法原地修改（如 `append`、`erase`）。

### 参数约定

- **位置参数**：`pos` 默认为容器尾部（`len()` 或 `end()`），插入操作在指定位置前插入。
- **区间参数**：`[left, right]` 表示闭区间，包含两端；所有索引从 0 开始。
- **比较函数**：`cmp` 接受两个参数，返回 `1` 表示前者优先于后者，否则保持顺序。

### 返回值与异常

- 方法通常返回 `None`（原地修改）或新对象（非原地操作）。

- 访问空容器（如 `front`、`pop`）或越界访问会抛出异常（`IndexError`、`EmptyError` 等）。

- `find` 返回 `-1` 表示未找到；`get` 支持返回默认值以避免异常。
  
  ---

根据您的要求，我们为类型系统新增了完整的**运算符**规范，包括优先级、结合性及各类运算符在不同类型上的语义。同时明确了 `~` 的重载（位取反 / memadr 解引用）和新增的 `@` 取地址运算符。以下是新增的运算符章节内容，建议插入到原文档“通用声明与操作”之后（可作为第0.5章或独立的第6章）。  

---

## 运算符

本语言支持丰富的运算符，包括算术、位运算、逻辑、比较、集合操作、拼接/重复、特殊地址操作及三目条件。运算符优先级遵循常规风格（从高到低），并可在必要时使用括号 `()` 改变计算顺序。

### 优先级与结合性（从高到低）

| 优先级 | 运算符                                                                  | 描述                    | 结合性   |
| --- | -------------------------------------------------------------------- | --------------------- | ----- |
| 1   | `()` `[]` `.`                                                        | 括号、下标、成员访问            | 左 → 右 |
| 2   | `@` `~` `!` `-` `+`                                                  | 一元取地址、解引用/位取反、逻辑非、正负号 | 右 → 左 |
| 3   | `**`                                                                 | 幂运算                   | 右 → 左 |
| 4   | `*` `/` `//` `%`                                                     | 乘、除、整除、取模             | 左 → 右 |
| 5   | `+` `-`                                                              | 加、减 / 拼接、差集           | 左 → 右 |
| 6   | `<<` `>>`                                                            | 左移、右移（仅整数）            | 左 → 右 |
| 7   | `&`                                                                  | 按位与 / 集合交集 / 布尔非短路与   | 左 → 右 |
| 8   | `^`                                                                  | 按位异或 / 集合对称差集         | 左 → 右 |
| 9   | `\|`                                                                 | 按位或 / 集合并集 / 布尔非短路或   | 左 → 右 |
| 10  | `==` `!=` `<` `>` `<=` `>=`                                          | 比较运算                  | 左 → 右 |
| 11  | `&&`                                                                 | 逻辑与（短路）               | 左 → 右 |
| 12  | `\|\|`                                                               | 逻辑或（短路）               | 左 → 右 |
| 13  | `? :`                                                                | 三目条件                  | 右 → 左 |
| 14  | `=` `+=` `-=` `*=` `/=` `//=` `%=` `**=` `&=` `\|=` `^=` `<<=` `>>=` | 赋值及复合赋值               | 右 → 左 |

> 注：逗号 `,` 不在上述列表中，仅用于参数列表或初始化列表，不属于运算符。

---

### 运算符详解

#### 1. 一元运算符

| 运算符  | 操作数类型        | 结果类型       | 语义                                                |
| ---- | ------------ | ---------- | ------------------------------------------------- |
| `@x` | 任何变量（左值）     | `memadr`   | 取变量 `x` 的内存地址（返回 `memadr`）。示例：`@a` 得到 `a` 的地址。    |
| `~x` | `memadr`     | 该地址指向的值的类型 | 解引用，读取/修改 `x` 指向的内存。示例：`~adr = 100;` 写入值。         |
| `~x` | 整数类型（`int`等） | 同类型        | 按位取反（每一位 0↔1）。示例：`~0b1010` → `0b...0101`（符号位也取反）。 |
| `!x` | `bool`       | `bool`     | 逻辑非：`true` → `false`，`false` → `true`。            |
| `!x` | 整数类型         | 同类型        | 按位取反（每一位 0↔1）。示例：`~0b1010` → `0b...0101`（符号位也取反）。 |
| `-x` | 数值类型         | 同类型        | 取负值（对于无符号整数会进行模运算）。                               |
| `+x` | 数值类型         | 同类型        | 正号，无效果（语法保留）。                                     |

#### 2. 算术运算符（二元）

| 运算符      | 操作数类型                    | 结果类型                 | 语义                                                          |
| -------- | ------------------------ | -------------------- | ----------------------------------------------------------- |
| `a ** b` | 数值                       | 数值（提升为 `double` 或相应） | 幂运算（`a` 的 `b` 次方）。                                          |
| `a * b`  | 数值                       | 数值                   | 乘法。                                                         |
| `a * b`  | `str` / `list` × `ulong` | 同类型                  | 重复操作：字符串或列表重复 `b` 次（`b` 为无符号整数）。示例：`"ab" * 3` → `"ababab"`。 |
| `a / b`  | 数值                       | `double`             | 浮点数除法（结果为双精度）。                                              |
| `a // b` | 数值                       | 整数（类型与操作数一致）         | 整除（向下取整，对负数向负无穷）。                                           |
| `a % b`  | 数值                       | 整数（同类型）              | 取模（结果与除数同号）。                                                |
| `a + b`  | 数值                       | 数值                   | 加法。                                                         |
| `a + b`  | `str` / `unistr`         | 同类型                  | 字符串拼接（返回新字符串）。                                              |
| `a + b`  | `list` / `tuple`（相同类型）   | 同类型                  | 容器拼接（返回新容器，元素按顺序合并）。                                        |
| `a - b`  | 数值                       | 数值                   | 减法。                                                         |

#### 3. 位运算符（仅整数类型）

| 运算符      | 语义                   |
| -------- | -------------------- |
| `a & b`  | 按位与（逐位 AND）          |
| `a \| b` | 按位或（逐位 OR）           |
| `a ^ b`  | 按位异或（逐位 XOR）         |
| `a << b` | 左移 `b` 位（低位补 0）      |
| `a >> b` | 右移 `b` 位（逻辑右移，高位补 0） |

#### 4. 逻辑运算符（短路与非短路）

| 运算符      | 操作数    | 结果     | 语义                                             |
| -------- | ------ | ------ | ---------------------------------------------- |
| `a & b`  | `bool` | `bool` | 逻辑与（短路）：若 `a` 为 `false` 则直接返回 `false`，不计算 `b`。 |
| `a \| b` | `bool` | `bool` | 逻辑或（短路）：若 `a` 为 `true` 则直接返回 `true`，不计算 `b`。   |
| `a && b` | `bool` | `bool` | 非短路逻辑与：同时计算 `a` 和 `b`，返回两者逻辑与。                 |
| `a \|    | b`     | `bool` | `bool`                                         |

> 非短路版本 `&&` 和 `||` 在操作数为布尔时优先于位运算，且不会短路。

#### 5. 集合运算符（仅适用于 `set` / `undset`）

| 运算符      | 语义                      | 示例                           |
| -------- | ----------------------- | ---------------------------- |
| `a & b`  | 交集（返回新集合）               | `{1,2} & {2,3}` → `{2}`      |
| `a \| b` | 并集（返回新集合）               | `{1,2} \| {2,3}` → `{1,2,3}` |
| `a ^ b`  | 对称差集（仅在其中一个集合中的元素）      | `{1,2} ^ {2,3}` → `{1,3}`    |
| `a - b`  | 差集（在 `a` 中但不在 `b` 中的元素） | `{1,2} - {2,3}` → `{1}`      |

#### 6. 比较运算符（所有可比较类型）

| 运算符      | 适用类型           | 语义                                 |
| -------- | -------------- | ---------------------------------- |
| `a == b` | 任何类型           | 相等：值相同（对于容器，逐元素比较；对于 `list`，顺序敏感）。 |
| `a != b` | 任何类型           | 不等：与 `==` 相反。                      |
| `a < b`  | 数值、字符、字符串（字典序） | 小于。                                |
| `a > b`  | 同上             | 大于。                                |
| `a <= b` | 同上             | 小于等于。                              |
| `a >= b` | 同上             | 大于等于。                              |

#### 7. 三目条件运算符

`condition ? expr1 : expr2`

- `condition` 必须为 `bool` 类型。
- 若 `condition` 为 `true`，结果为 `expr1` 的值；否则为 `expr2` 的值。
- `expr1` 和 `expr2` 的类型应兼容（或可隐式转换），否则会引发编译错误。

#### 8. 赋值与复合赋值

| 运算符       | 语义                           |
| --------- | ---------------------------- |
| `a = b`   | 将 `b` 的值赋给 `a`（`a` 必须为左值）。   |
| `a += b`  | 等价于 `a = a + b`（但只计算一次 `a`）。 |
| `a -= b`  | `a = a - b`                  |
| `a *= b`  | `a = a * b`                  |
| `a /= b`  | `a = a / b`                  |
| `a //= b` | `a = a // b`                 |
| `a %= b`  | `a = a % b`                  |
| `a **= b` | `a = a ** b`                 |
| `a &= b`  | `a = a & b`（整数位与，或集合交集）      |
| `a \|= b` | `a = a \| b`（整数位或，或集合并集）     |
| `a ^= b`  | `a = a ^ b`（整数异或，或集合对称差）     |
| `a <<= b` | `a = a << b`（仅整数）            |
| `a >>= b` | `a = a >> b`（仅整数）            |

> 复合赋值中的 `&=`、`|=`、`^=` 会根据 `a` 的类型自动选择位运算或集合运算。

---

### 运算符与 `memadr` 的整合

- `@x` 取变量地址，返回 `memadr`。

- `~adr` 解引用 `memadr`，可读取或修改指向的内存。

- 对于整数，`~` 仍作为按位取反，与 `~adr` 通过操作数类型区分，不产生歧义。

- 示例：
  
  ```
  int a = 10;                 # 声明整数
  memadr p = @a;              # 取地址
  ~p = 20;                    # 修改 a 的值
  int b = ~p;                 # 读取 a 的值，b = 20
  int c = ~0b1010;            # 按位取反，c 为二进制 ...0101
  ```

---

### 使用示例（含注释）

```
# 算术与幂
int x = 2 + 3 * 4;             # 14（* 优先）
double y = 2 ** 10;            # 1024.0

# 字符串与列表拼接
str s = "Hello" + " " + "World";   # "Hello World"
list l = [1, 2] + [3, 4];          # [1, 2, 3, 4]
list r = [0] * 5;                  # [0, 0, 0, 0, 0]

# 集合运算
set a = {1, 2, 3};
set b = {3, 4, 5};
set inter = a & b;                 # {3}
set union = a | b;                 # {1,2,3,4,5}
set diff = a - b;                  # {1,2}
set sym = a ^ b;                   # {1,2,4,5}

# 比较与三目
bool ok = (x > 10) ? true : false;

# 逻辑与短路
if (a != None && a.len() > 0) { ... }   # 短路保护，不会在 a 为 None 时访问 len

# 位运算
int flags = 0b1100;
int masked = flags & 0b1010;       # 0b1000

# 取地址与解引用
int val = 42;
memadr addr = @val;
~addr = 100;                       # val 变为 100
print(~addr);                      # 输出 100
```

---

### 注意事项

- **类型提升**：算术运算中，若操作数类型不同，会进行隐式提升（如 `int` + `float` → `float`）。
- **集合运算**仅对 `set` 和 `undset` 有效，对其他类型使用 `&`、`|`、`^`、`-` 会按位运算或数值运算处理。
- **字符串/列表重复**：`*` 右侧必须为无符号整数，否则编译报错。
- **短路逻辑** `&&` 和 `||` 仅用于布尔表达式，且不重载。
- **非短路逻辑** `&` 和 `|` 在布尔上下文中仍会计算两个操作数，适合用于需要副作用的情况（但应谨慎）。
- **赋值运算符**的左侧必须为可修改的左值（变量或可写内存地址）。

---

## 1. 数值类型

### 1.1 整数类型

| 类型名      | 全称                     | 范围                     | 说明          |
| -------- | ---------------------- | ---------------------- | ----------- |
| `int`    | integer                | $-2^{31}$ ~ $2^{31}-1$ | 标准有符号32位整数。 |
| `uint`   | unsigned integer       | $0$ ~ $2^{32}-1$       | 标准无符号32位整数。 |
| `short`  | short integer          | $-2^{15}$ ~ $2^{15}-1$ | 有符号16位整数。   |
| `ushort` | unsigned short integer | $0$ ~ $2^{16}-1$       | 无符号16位整数。   |
| `long`   | long integer           | $-2^{63}$ ~ $2^{63}-1$ | 有符号64位整数。   |
| `ulong`  | unsigned long integer  | $0$ ~ $2^{64}-1$       | 无符号64位整数。   |

### 1.2 浮点类型

| 类型名      | 全称                 | 标准       | 大约范围                                        | 说明           |
| -------- | ------------------ | -------- | ------------------------------------------- | ------------ |
| `float`  | (single-precision) | IEEE-754 | $2^{-126}$ ~ $(2-2^{-23}) \times 2^{127}$   | 单精度浮点数。      |
| `double` | (double-precision) | IEEE-754 | $2^{-1022}$ ~ $(2-2^{-52}) \times 2^{1023}$ | 双精度浮点数，精度更高。 |

### 1.3 字符与地址类型

| 类型名       | 全称                | 表示范围 / 说明                          | 存储空间    |
| --------- | ----------------- | ---------------------------------- | ------- |
| `char`    | ASCII character   | 0~255（ASCII表）                      | 8 bits  |
| `unichar` | Unicode character | `(0x00)` ~ `(0x10FFFF)`（Unicode码点） | 32 bits |
| `memadr`  | memory address    | 存储任意变量的内存地址                        | 平台相关    |

#### `memadr` 操作详解

`memadr` 提供底层内存操作能力（**请谨慎使用**）。

| 操作      | 语法                | 说明                              |
| ------- | ----------------- | ------------------------------- |
| 获取地址    | `memadr adr = a;` | 将变量 `a` 的地址存储到 `adr` 中。         |
| 修改指向    | `adr = b;`        | 使 `adr` 指向变量 `b` 的地址。           |
| 删除指向的内存 | `del ~adr;`       | **危险**：释放 `adr` 指向的内存，可能导致悬挂指针。 |
| 删除地址变量  | `del adr;`        | 仅删除 `adr` 变量本身，不影响其指向的内存。       |
| 读取指向的值  | `print(~adr);`    | 读取 `adr` 指向地址中存储的值。             |
| 修改指向的值  | `~adr = 1000;`    | 修改 `adr` 指向地址中存储的值。             |

### 1.4 布尔类型

| 类型名    | 全称      | 取值               | 说明                  |
| ------ | ------- | ---------------- | ------------------- |
| `bool` | boolean | `true` / `false` | 逻辑布尔值，支持与（`&&`）、或（` |

---

## 2. 字符串与二进制类型

### 2.1 `str` (ASCII 字符串)

- 元素为 **ASCII** 字符 (0~255)。
- 长度无限（受内存限制）。
- 支持 `[]` 运算符访问和修改任意位置的字符（索引从 0 开始）。

| 方法        | 参数                                     | 返回值     | 说明                            |
| --------- | -------------------------------------- | ------- | ----------------------------- |
| `append`  | `(str string, ulong pos = self.len())` | `None`  | 在 `pos` 位置插入 `string`。        |
| `append`  | `(char c, ulong count, ulong pos)`     | `None`  | 在 `pos` 位置插入 `count` 个字符 `c`。 |
| `erase`   | `(ulong left, ulong right)`            | `None`  | 删除区间 `[left, right]` 内的字符。    |
| `len`     | `()`                                   | `ulong` | 返回字符串长度。                      |
| `find`    | `(str string)`                         | `long`  | 返回子串首次出现的起始索引；若未找到，返回 `-1`。   |
| `count`   | `(str s)`                              | `ulong` | 返回子串 `s` 出现的次数。               |
| `count`   | `(char c)`                             | `ulong` | 返回字符 `c` 出现的次数。               |
| `replace` | `(str a, str b)`                       | `None`  | 将所有出现的子串 `a` 替换为 `b`。         |
| `replace` | `(ulong l, ulong r, str string)`       | `None`  | 将区间 `[l, r]` 替换为 `string`。    |
| `slice`   | `(ulong start, ulong end)`             | `str`   | 返回子串 `[start, end)`。          |
| `to_list` | `()`                                   | `list`  | 返回由每个字符组成的列表。                 |

### 2.2 `unistr` (Unicode 字符串)

- 元素为 **Unicode** 字符 (码点 `0x00` ~ `0x10FFFF`)。
- 长度无限（受内存限制）。
- 支持 `[]` 运算符访问和修改任意位置的字符（索引从 0 开始）。

| 方法        | 参数                                        | 返回值      | 说明                            |
| --------- | ----------------------------------------- | -------- | ----------------------------- |
| `append`  | `(unistr string, ulong pos = self.len())` | `None`   | 在 `pos` 位置插入 `string`。        |
| `append`  | `(unichar c, ulong count, ulong pos)`     | `None`   | 在 `pos` 位置插入 `count` 个字符 `c`。 |
| `erase`   | `(ulong left, ulong right)`               | `None`   | 删除区间 `[left, right]` 内的字符。    |
| `len`     | `()`                                      | `ulong`  | 返回字符串长度。                      |
| `find`    | `(unistr string)`                         | `long`   | 返回子串首次出现的起始索引；若未找到，返回 `-1`。   |
| `count`   | `(unistr s)`                              | `ulong`  | 返回子串 `s` 出现的次数。               |
| `count`   | `(unichar c)`                             | `ulong`  | 返回字符 `c` 出现的次数。               |
| `replace` | `(unistr a, unistr b)`                    | `None`   | 将所有出现的子串 `a` 替换为 `b`。         |
| `replace` | `(ulong l, ulong r, unistr string)`       | `None`   | 将区间 `[l, r]` 替换为 `string`。    |
| `slice`   | `(ulong start, ulong end)`                | `unistr` | 返回子串 `[start, end)`。          |
| `to_list` | `()`                                      | `list`   | 返回由每个字符组成的列表。                 |

### 2.3 `bin` (二进制字节序列)

`bin` 表示一个原始的字节序列，每个字节为无符号8位整数（0~255）。它支持按字节索引访问、切片、拼接及多种检索操作。

#### 声明与初始化

```
bin b;                              # 声明一个空二进制对象
bin b = [0x00, 0xFF, 0x7F];         # 以字节列表初始化
bin b = "hello";                    # 从ASCII字符串初始化（按字节编码）
bin b = {0x48, 0x65};               # 另一种初始化方式（花括号）
```

#### 方法列表

| 方法        | 参数                                     | 返回值      | 说明                              |
| --------- | -------------------------------------- | -------- | ------------------------------- |
| `append`  | `(ulong byte, ulong pos = self.len())` | `None`   | 在 `pos` 位置插入一个字节（0~255）。        |
| `append`  | `(bin bytes, ulong pos = self.len())`  | `None`   | 在 `pos` 位置插入另一个二进制对象。           |
| `erase`   | `(ulong left, ulong right)`            | `None`   | 删除区间 `[left, right]` 内的字节。      |
| `len`     | `()`                                   | `ulong`  | 返回字节数。                          |
| `find`    | `(bin pattern)`                        | `long`   | 返回模式首次出现的起始索引；未找到返回 `-1`。       |
| `count`   | `(bin pattern)`                        | `ulong`  | 返回模式出现的次数。                      |
| `replace` | `(bin old, bin new)`                   | `None`   | 将所有出现的 `old` 替换为 `new`。         |
| `replace` | `(ulong l, ulong r, bin bytes)`        | `None`   | 将区间 `[l, r]` 替换为 `bytes`。       |
| `slice`   | `(ulong start, ulong end)`             | `bin`    | 返回子区间 `[start, end)` 的副本。       |
| `hex`     | `()`                                   | `str`    | 返回十六进制字符串表示（如 `"FFA0"`）。        |
| `decode`  | `(str encoding = "utf-8")`             | `unistr` | 按指定编码解码为 Unicode 字符串（默认 UTF-8）。 |
| `encode`  | `(unistr s, str encoding = "utf-8")`   | `bin`    | 将 Unicode 字符串编码为二进制（静态方法）。      |

---

## 3. 列表类型 (`list`)

`list` 是一个通用的、可动态增长的**双向链表**，可存储任意类型的元素。

### 定义与初始化

```
list a;             # 声明一个空列表
list a = [1, 5];    # 声明并初始化为包含元素 1 和 5 的列表
```

### 方法列表（含新增常用方法）

| 方法         | 参数                                                                                       | 返回值      | 说明                                                                   |
| ---------- | ---------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------- |
| `append`   | `(object t, ulong pos = self.end())`                                                     | `None`   | 在 `pos` 位置插入元素 `t`（默认尾部）。                                            |
| `append`   | `(object t, ulong count, ulong pos)`                                                     | `None`   | 在 `pos` 位置插入 `count` 个元素 `t`。                                        |
| `insert`   | `(ulong pos, object t)`                                                                  | `None`   | 在 `pos` 位置插入一个元素（等效于 `append(t, pos)`）。                              |
| `erase`    | `(ulong left, ulong right)`                                                              | `None`   | 删除区间 `[left, right]` 内的所有元素。                                         |
| `remove`   | `(object t)`                                                                             | `bool`   | 删除第一个等于 `t` 的元素，成功返回 `true`，否则返回 `false`。                            |
| `len`      | `()`                                                                                     | `ulong`  | 返回列表的元素个数。                                                           |
| `begin`    | `()`                                                                                     | `ulong`  | 返回起始下标（固定为 `0`）。                                                     |
| `end`      | `()`                                                                                     | `ulong`  | 返回尾部位置（即元素个数）。                                                       |
| `clear`    | `()`                                                                                     | `None`   | 清空列表中的所有元素。                                                          |
| `contains` | `(object t)`                                                                             | `bool`   | 若列表包含 `t` 返回 `true`，否则 `false`。                                      |
| `find`     | `(object t)`                                                                             | `long`   | 返回元素 `t` 首次出现的索引；若未找到，返回 `-1`。                                       |
| `count`    | `(object s)`                                                                             | `ulong`  | 返回元素 `s` 在列表中出现的次数。                                                  |
| `replace`  | `(object a, object b)`                                                                   | `None`   | 将所有等于 `a` 的元素替换为 `b`。                                                |
| `replace`  | `(ulong l, ulong r, list lst)`                                                           | `None`   | 将区间 `[l, r]` 替换为列表 `lst`。                                            |
| `reverse`  | `()`                                                                                     | `None`   | 反转列表的元素顺序。                                                           |
| `slice`    | `(ulong start, ulong end)`                                                               | `list`   | 返回子列表 `[start, end)` 的副本。                                            |
| `sort`     | `(ulong l = self.begin(), ulong r = self.end(), function cmp = (a, b) => a < b ? 1 : 0)` | `None`   | 对区间 `[l, r]` 按比较函数 `cmp` 排序。`cmp` 返回 `1` 表示 `a` 应排在 `b` 之前，否则保持相对顺序。 |
| `shuffle`  | `(ulong l = self.begin(), ulong r = self.end(), seed = __SEED)`                          | `None`   | 使用种子 `seed` 打乱区间 `[l, r]` 内的元素。                                      |
| `sum`      | `(ulong l = self.begin(), ulong r = self.end())`                                         | `long`   | 计算区间 `[l, r]` 内所有元素的和（元素需支持加法运算）。                                    |
| `mean`     | `(ulong l = self.begin(), ulong r = self.end(), ulong decimal-precision = 15)`           | `double` | 计算区间 `[l, r]` 内元素的平均值，结果保留 `decimal-precision` 位小数。                  |
| `copy`     | `()`                                                                                     | `list`   | 返回列表的浅拷贝（元素引用相同）。                                                    |

---

## 4. 容器类型

以下容器类型均支持存储任意类型的元素（但 `set`、`undset`、`dict` 对键类型有特定要求）。所有容器均已内置 `clear()` 方法。

### 4.1 栈 (`stack`)

`stack` 是一个后进先出（LIFO）的容器，基于动态数组或链表实现，大小无上限。

#### 声明与初始化

```
stack s;                # 声明一个空栈
stack s = [1, 2, 3];    # 声明并初始化为包含 1,2,3 的栈（栈顶为最后一个元素）
```

#### 方法列表

| 方法         | 参数           | 返回值      | 说明                                      |
| ---------- | ------------ | -------- | --------------------------------------- |
| `push`     | `(object t)` | `None`   | 将元素 `t` 压入栈顶。                           |
| `pop`      | `()`         | `object` | 弹出栈顶元素并返回该元素（若栈为空则抛出异常）。                |
| `top`      | `()`         | `object` | 返回栈顶元素的引用（若栈为空则抛出异常）。                   |
| `empty`    | `()`         | `bool`   | 若栈为空返回 `true`，否则返回 `false`。             |
| `len`      | `()`         | `ulong`  | 返回栈中元素个数。                               |
| `clear`    | `()`         | `None`   | 清空栈中所有元素。                               |
| `contains` | `(object t)` | `bool`   | 若栈中包含元素 `t` 返回 `true`，否则 `false`（顺序搜索）。 |
| `copy`     | `()`         | `stack`  | 返回栈的浅拷贝。                                |

---

### 4.2 队列 (`queue`)

`queue` 是一个先进先出（FIFO）的容器，基于动态数组或链表实现，大小无上限。

#### 声明与初始化

```
queue q;                # 声明一个空队列
queue q = [1, 2, 3];    # 声明并初始化为包含 1,2,3 的队列（队首为第一个元素）
```

#### 方法列表

| 方法         | 参数           | 返回值      | 说明                                       |
| ---------- | ------------ | -------- | ---------------------------------------- |
| `push`     | `(object t)` | `None`   | 将元素 `t` 入队（添加到队尾）。                       |
| `pop`      | `()`         | `object` | 弹出队首元素并返回该元素（若队列为空则抛出异常）。                |
| `front`    | `()`         | `object` | 返回队首元素的引用（若队列为空则抛出异常）。                   |
| `back`     | `()`         | `object` | 返回队尾元素的引用（若队列为空则抛出异常）。                   |
| `empty`    | `()`         | `bool`   | 若队列为空返回 `true`，否则返回 `false`。             |
| `len`      | `()`         | `ulong`  | 返回队列中元素个数。                               |
| `clear`    | `()`         | `None`   | 清空队列中所有元素。                               |
| `contains` | `(object t)` | `bool`   | 若队列中包含元素 `t` 返回 `true`，否则 `false`（顺序搜索）。 |
| `copy`     | `()`         | `queue`  | 返回队列的浅拷贝。                                |

---

### 4.3 有序集合 (`set`)

`set` 是一个有序容器，元素唯一且自动按升序排列（基于 `<` 比较）。元素类型必须支持比较运算符。

#### 声明与初始化

```
set s;                  # 声明一个空集合
set s = [3, 1, 2];      # 声明并初始化为 {1, 2, 3}（自动排序去重）
```

#### 方法列表（含集合运算）

| 方法                     | 参数            | 返回值      | 说明                                           |
| ---------------------- | ------------- | -------- | -------------------------------------------- |
| `insert`               | `(object t)`  | `bool`   | 插入元素 `t`，若已存在则不操作并返回 `false`，否则插入并返回 `true`。 |
| `erase`                | `(object t)`  | `bool`   | 删除元素 `t`，若存在则删除并返回 `true`，否则返回 `false`。      |
| `contains`             | `(object t)`  | `bool`   | 若元素 `t` 存在返回 `true`，否则返回 `false`。            |
| `empty`                | `()`          | `bool`   | 若集合为空返回 `true`，否则返回 `false`。                 |
| `len`                  | `()`          | `ulong`  | 返回集合中元素个数。                                   |
| `clear`                | `()`          | `None`   | 清空集合中所有元素。                                   |
| `min`                  | `()`          | `object` | 返回最小元素（若为空则抛出异常）。                            |
| `max`                  | `()`          | `object` | 返回最大元素（若为空则抛出异常）。                            |
| `lower_bound`          | `(object t)`  | `object` | 返回第一个不小于 `t` 的元素（若不存在则抛出异常）。                 |
| `upper_bound`          | `(object t)`  | `object` | 返回第一个大于 `t` 的元素（若不存在则抛出异常）。                  |
| `count`                | `(object t)`  | `ulong`  | 返回元素 `t` 的个数（因为集合唯一，结果总是 0 或 1）。             |
| `union`                | `(set other)` | `set`    | 返回当前集合与 `other` 的并集。                         |
| `intersection`         | `(set other)` | `set`    | 返回当前集合与 `other` 的交集。                         |
| `difference`           | `(set other)` | `set`    | 返回当前集合与 `other` 的差集（当前中有而 other 中没有）。        |
| `symmetric_difference` | `(set other)` | `set`    | 返回对称差集（仅在其中一个集合中出现的元素）。                      |
| `is_subset`            | `(set other)` | `bool`   | 若当前集合是 `other` 的子集返回 `true`。                 |
| `is_superset`          | `(set other)` | `bool`   | 若当前集合是 `other` 的超集返回 `true`。                 |
| `is_disjoint`          | `(set other)` | `bool`   | 若两个集合无共同元素返回 `true`。                         |
| `copy`                 | `()`          | `set`    | 返回集合的副本。                                     |

---

### 4.4 无序集合 (`undset`)

`undset` 即无序集合（哈希集合），元素唯一但不保证顺序。元素类型必须支持哈希计算和相等比较。

#### 声明与初始化

```
undset us;              # 声明一个空无序集合
undset us = [3, 1, 2];  # 声明并初始化为包含 3,1,2 的集合（顺序不定）
```

#### 方法列表（与 `set` 类似，但不保证顺序）

| 方法                     | 参数                         | 返回值      | 说明                                           |
| ---------------------- | -------------------------- | -------- | -------------------------------------------- |
| `insert`               | `(object t)`               | `bool`   | 插入元素 `t`，若已存在则不操作并返回 `false`，否则插入并返回 `true`。 |
| `erase`                | `(object t)`               | `bool`   | 删除元素 `t`，若存在则删除并返回 `true`，否则返回 `false`。      |
| `contains`             | `(object t)`               | `bool`   | 若元素 `t` 存在返回 `true`，否则返回 `false`。            |
| `empty`                | `()`                       | `bool`   | 若集合为空返回 `true`，否则返回 `false`。                 |
| `len`                  | `()`                       | `ulong`  | 返回集合中元素个数。                                   |
| `clear`                | `()`                       | `None`   | 清空集合中所有元素。                                   |
| `rehash`               | `(ulong new_bucket_count)` | `None`   | 调整哈希表桶数，用于优化性能。                              |
| `union`                | `(undset other)`           | `undset` | 返回并集。                                        |
| `intersection`         | `(undset other)`           | `undset` | 返回交集。                                        |
| `difference`           | `(undset other)`           | `undset` | 返回差集。                                        |
| `symmetric_difference` | `(undset other)`           | `undset` | 返回对称差集。                                      |
| `is_subset`            | `(undset other)`           | `bool`   | 子集判断。                                        |
| `is_superset`          | `(undset other)`           | `bool`   | 超集判断。                                        |
| `is_disjoint`          | `(undset other)`           | `bool`   | 不相交判断。                                       |
| `copy`                 | `()`                       | `undset` | 返回副本。                                        |

---

### 4.5 字典 (`dict`)

`dict` 是一种键值对映射容器，键唯一且无序（基于哈希表实现）。键类型必须支持哈希和相等比较。

#### 声明与初始化

```
dict d;                              # 声明一个空字典
dict d = { "name": "Alice", "age": 30 };   # 键值对初始化
dict d = [("key1", 1), ("key2", 2)];       # 从元组列表初始化
```

#### 方法列表（含常用操作）

| 方法           | 参数                                    | 返回值                | 说明                                        |
| ------------ | ------------------------------------- | ------------------ | ----------------------------------------- |
| `put`        | `(object key, object value)`          | `None`             | 插入或更新键 `key` 对应的值为 `value`。               |
| `get`        | `(object key)`                        | `object`           | 返回键 `key` 对应的值；若不存在则抛出异常。                 |
| `get`        | `(object key, object default)`        | `object`           | 返回键 `key` 对应的值；若不存在则返回 `default`。         |
| `has`        | `(object key)`                        | `bool`             | 若键 `key` 存在返回 `true`，否则 `false`。          |
| `erase`      | `(object key)`                        | `bool`             | 删除键 `key` 及其值，成功返回 `true`，否则 `false`。     |
| `pop`        | `(object key)`                        | `object`           | 删除键 `key` 并返回其值；若不存在则抛出异常。                |
| `pop`        | `(object key, object default)`        | `object`           | 删除键 `key` 并返回其值；若不存在则返回 `default`。        |
| `popitem`    | `()`                                  | `(object, object)` | 随机删除并返回一个键值对（若字典为空则抛出异常）。                 |
| `setdefault` | `(object key, object default = None)` | `object`           | 若键存在返回其值，否则插入键并设为 `default`，返回 `default`。 |
| `keys`       | `()`                                  | `list`             | 返回所有键组成的列表（顺序不定）。                         |
| `values`     | `()`                                  | `list`             | 返回所有值组成的列表（顺序与键一致）。                       |
| `items`      | `()`                                  | `list`             | 返回所有 `(key, value)` 元组组成的列表。              |
| `len`        | `()`                                  | `ulong`            | 返回键值对个数。                                  |
| `empty`      | `()`                                  | `bool`             | 若字典为空返回 `true`，否则 `false`。                |
| `clear`      | `()`                                  | `None`             | 清空所有键值对。                                  |
| `update`     | `(dict other)`                        | `None`             | 将另一个字典 `other` 的键值对合并进来（覆盖相同键）。           |
| `copy`       | `()`                                  | `dict`             | 返回字典的浅拷贝。                                 |
| `fromkeys`   | `(list keys, object default = None)`  | `dict`             | 静态方法，从键列表创建字典，所有值初始化为 `default`。          |

---

## 5. 复合类型

复合类型用于将多个值组合为一个结构。它们固定长度，且每个元素的类型在声明时指定。

### 5.1 对 (`pair`)

`pair` 是一个包含两个元素的复合类型，两个元素的类型可以不同，但需在声明时明确指定。

#### 声明与初始化

```
pair<type1, type2> p;              # 声明一个 pair，元素类型为 type1 和 type2
pair<int, str> p = {42, "hello"};  # 声明并初始化
pair<float, bool> p(3.14, true);   # 使用构造函数初始化
```

#### 访问元素

- 使用 `.first` 访问第一个元素。
- 使用 `.second` 访问第二个元素。

示例：

```
pair<int, str> p = {10, "world"};
int a = p.first;      # a = 10
str s = p.second;     # s = "world"
```

#### 方法

| 方法          | 参数                   | 返回值                  | 说明                      |
| ----------- | -------------------- | -------------------- | ----------------------- |
| `swap`      | `(pair other)`       | `None`               | 交换两个 pair 的内容（类型必须相同）。  |
| `make_pair` | `(type1 a, type2 b)` | `pair<type1, type2>` | 全局函数，用于创建 pair（类型自动推导）。 |

### 5.2 元组 (`tuple`)

`tuple` 是一个包含固定数量元素的复合类型，每个元素的类型可以不同，需在声明时指定完整的类型列表。

#### 声明与初始化

```
tuple<type1, type2, ...> t;                         # 声明空元组（可后续赋值）
tuple<int, str, float> t = {10, "hello", 3.14};    # 声明并初始化
tuple<int, double> t(5, 2.5);                      # 使用构造函数
```

#### 访问元素

- 使用模板化的 `get<index>(t)` 函数（索引从 0 开始）。
- 或者使用 `.get(index)` 方法（若支持）。

示例：

```
tuple<int, str, float> t = {42, "answer", 0.5};
int i = get<0>(t);      # i = 42
str s = get<1>(t);      # s = "answer"
float f = get<2>(t);    # f = 0.5
```

#### 方法

| 方法           | 参数               | 返回值          | 说明                           |
| ------------ | ---------------- | ------------ | ---------------------------- |
| `len`        | `()`             | `ulong`      | 返回元组中元素的个数（编译期常量）。           |
| `get`        | `(ulong index)`  | `object`     | 返回第 `index` 个元素的引用（运行期检查越界）。 |
| `swap`       | `(tuple other)`  | `None`       | 交换两个 tuple 的内容（类型必须完全一致）。    |
| `make_tuple` | `(a, b, c, ...)` | `tuple<...>` | 全局函数，根据参数自动推导类型创建元组。         |

---

## 使用说明

- **常量**：使用 `const` 声明的变量必须在声明时初始化，且之后不可修改。
- **字面量**：整数字面量可使用二进制（`0b`）、八进制（`0o`）、十六进制（`0x`）前缀；浮点数也支持十六进制表示。
- **复合类型**：`pair` 和 `tuple` 的元素类型在声明时必须完整指定，不支持类型推导（除非使用 `make_pair`/`make_tuple` 辅助函数）。
- **容器类型**（`list`、`stack`、`queue`、`set`、`undset`、`dict`）均不提供 `[]` 运算符进行随机访问（除 `str`、`unistr`、`bin` 外）。
- 对于 `set` 和 `undset`，元素类型必须满足相应的条件（可比较或可哈希），否则编译或运行时会报错。
- 对于 `dict`，键类型必须支持哈希和相等比较。
- 所有带有 `pos` 参数的方法，其默认值均为容器的尾部位置（即 `len()` 或 `end()`），确保插入操作默认在末尾进行。
- **注释**：所有代码示例中的注释均使用 `#` 符号，符合本语言规范。

## 注释规范

代码中使用 `#` 作为注释符号，支持行尾注释和独立注释行：

```
# 这是一个独立注释行
int a = 10;       # 这是一个行尾注释
del a;            # 删除变量
```

注释内容会被编译器忽略，仅用于开发者阅读。

---

## 通用声明与操作

所有变量遵循以下声明和操作规则：

| 操作     | 语法                        | 说明                      |
| ------ | ------------------------- | ----------------------- |
| 声明变量   | `typename a;`             | 声明一个指定类型的变量，初始值由其默认值确定。 |
| 赋值     | `a = ...;`                | 将新值赋给已声明的变量。            |
| 声明并初始化 | `typename b = ...;`       | 声明变量的同时设置初始值。           |
| 同时声明   | `typename c, d;`          | 在一行中声明多个同类型变量。          |
| 声明常量   | `const typename e = ...;` | 声明一个常量，初始化后不可修改。        |
| 删除变量   | `del a;`                  | 删除指定的变量，释放其占用的资源。       |
| 删除多个变量 | `del b, c;`               | 同时删除多个变量。               |
| 删除所有变量 | `del *;`                  | **危险操作**：删除当前作用域内的所有变量。 |

### 字面量格式

整数字面量支持以下进制表示（所有进制数值均存储为相应的整数类型）：

| 进制   | 前缀   | 示例             | 说明               |
| ---- | ---- | -------------- | ---------------- |
| 二进制  | `0b` | `0b1010`       | 等价于十进制 10。       |
| 八进制  | `0o` | `0o12`         | 等价于十进制 10。       |
| 十六进制 | `0x` | `0xA` 或 `0xFF` | 等价于十进制 10 或 255。 |

浮点数也支持十六进制表示（符合 IEEE-754），例如 `0x1.2p3` 表示 `(1 + 2/16) * 2^3 = 9.0`。

---

## 容器通用操作语法规范

所有容器类型（`list`、`stack`、`queue`、`set`、`undset`、`dict`）遵循以下统一的语法和行为规范：

### 声明与初始化

- 空容器：`typename container;`（默认构造为空）。
- 使用初始化列表：`typename container = [elem1, elem2, ...];`（对于 `dict`，使用 `{key: value, ...}` 或 `[(key, value), ...]`）。

### 方法调用

- 所有方法通过 `.` 运算符调用：`container.method(arg1, arg2, ...);`
- 方法支持默认参数（如 `pos` 默认为尾部）。
- 部分方法返回新对象（如复制、集合运算），部分方法原地修改（如 `append`、`erase`）。

### 参数约定

- **位置参数**：`pos` 默认为容器尾部（`len()` 或 `end()`），插入操作在指定位置前插入。
- **区间参数**：`[left, right]` 表示闭区间，包含两端；所有索引从 0 开始。
- **比较函数**：`cmp` 接受两个参数，返回 `1` 表示前者优先于后者，否则保持顺序。

### 返回值与异常

- 方法通常返回 `None`（原地修改）或新对象（非原地操作）。

- 访问空容器（如 `front`、`pop`）或越界访问会抛出异常（`IndexError`、`EmptyError` 等）。

- `find` 返回 `-1` 表示未找到；`get` 支持返回默认值以避免异常。
  
  ---

根据您的要求，我们为类型系统新增了完整的**运算符**规范，包括优先级、结合性及各类运算符在不同类型上的语义。同时明确了 `~` 的重载（位取反 / memadr 解引用）和新增的 `@` 取地址运算符。以下是新增的运算符章节内容，建议插入到原文档“通用声明与操作”之后（可作为第0.5章或独立的第6章）。  

---

## 运算符

本语言支持丰富的运算符，包括算术、位运算、逻辑、比较、集合操作、拼接/重复、特殊地址操作及三目条件。运算符优先级遵循常规风格（从高到低），并可在必要时使用括号 `()` 改变计算顺序。

### 优先级与结合性（从高到低）

| 优先级 | 运算符                                                                  | 描述                    | 结合性   |
| --- | -------------------------------------------------------------------- | --------------------- | ----- |
| 1   | `()` `[]` `.`                                                        | 括号、下标、成员访问            | 左 → 右 |
| 2   | `@` `~` `!` `-` `+`                                                  | 一元取地址、解引用/位取反、逻辑非、正负号 | 右 → 左 |
| 3   | `**`                                                                 | 幂运算                   | 右 → 左 |
| 4   | `*` `/` `//` `%`                                                     | 乘、除、整除、取模             | 左 → 右 |
| 5   | `+` `-`                                                              | 加、减 / 拼接、差集           | 左 → 右 |
| 6   | `<<` `>>`                                                            | 左移、右移（仅整数）            | 左 → 右 |
| 7   | `&`                                                                  | 按位与 / 集合交集 / 布尔非短路与   | 左 → 右 |
| 8   | `^`                                                                  | 按位异或 / 集合对称差集         | 左 → 右 |
| 9   | `\|`                                                                 | 按位或 / 集合并集 / 布尔非短路或   | 左 → 右 |
| 10  | `==` `!=` `<` `>` `<=` `>=`                                          | 比较运算                  | 左 → 右 |
| 11  | `&&`                                                                 | 逻辑与（短路）               | 左 → 右 |
| 12  | `\|\|`                                                               | 逻辑或（短路）               | 左 → 右 |
| 13  | `? :`                                                                | 三目条件                  | 右 → 左 |
| 14  | `=` `+=` `-=` `*=` `/=` `//=` `%=` `**=` `&=` `\|=` `^=` `<<=` `>>=` | 赋值及复合赋值               | 右 → 左 |

> 注：逗号 `,` 不在上述列表中，仅用于参数列表或初始化列表，不属于运算符。

---

### 运算符详解

#### 1. 一元运算符

| 运算符  | 操作数类型        | 结果类型       | 语义                                                |
| ---- | ------------ | ---------- | ------------------------------------------------- |
| `@x` | 任何变量（左值）     | `memadr`   | 取变量 `x` 的内存地址（返回 `memadr`）。示例：`@a` 得到 `a` 的地址。    |
| `~x` | `memadr`     | 该地址指向的值的类型 | 解引用，读取/修改 `x` 指向的内存。示例：`~adr = 100;` 写入值。         |
| `~x` | 整数类型（`int`等） | 同类型        | 按位取反（每一位 0↔1）。示例：`~0b1010` → `0b...0101`（符号位也取反）。 |
| `!x` | `bool`       | `bool`     | 逻辑非：`true` → `false`，`false` → `true`。            |
| `!x` | 整数类型         | 同类型        | 按位取反（每一位 0↔1）。示例：`~0b1010` → `0b...0101`（符号位也取反）。 |
| `-x` | 数值类型         | 同类型        | 取负值（对于无符号整数会进行模运算）。                               |
| `+x` | 数值类型         | 同类型        | 正号，无效果（语法保留）。                                     |

#### 2. 算术运算符（二元）

| 运算符      | 操作数类型                    | 结果类型                 | 语义                                                          |
| -------- | ------------------------ | -------------------- | ----------------------------------------------------------- |
| `a ** b` | 数值                       | 数值（提升为 `double` 或相应） | 幂运算（`a` 的 `b` 次方）。                                          |
| `a * b`  | 数值                       | 数值                   | 乘法。                                                         |
| `a * b`  | `str` / `list` × `ulong` | 同类型                  | 重复操作：字符串或列表重复 `b` 次（`b` 为无符号整数）。示例：`"ab" * 3` → `"ababab"`。 |
| `a / b`  | 数值                       | `double`             | 浮点数除法（结果为双精度）。                                              |
| `a // b` | 数值                       | 整数（类型与操作数一致）         | 整除（向下取整，对负数向负无穷）。                                           |
| `a % b`  | 数值                       | 整数（同类型）              | 取模（结果与除数同号）。                                                |
| `a + b`  | 数值                       | 数值                   | 加法。                                                         |
| `a + b`  | `str` / `unistr`         | 同类型                  | 字符串拼接（返回新字符串）。                                              |
| `a + b`  | `list` / `tuple`（相同类型）   | 同类型                  | 容器拼接（返回新容器，元素按顺序合并）。                                        |
| `a - b`  | 数值                       | 数值                   | 减法。                                                         |

#### 3. 位运算符（仅整数类型）

| 运算符      | 语义                   |
| -------- | -------------------- |
| `a & b`  | 按位与（逐位 AND）          |
| `a \| b` | 按位或（逐位 OR）           |
| `a ^ b`  | 按位异或（逐位 XOR）         |
| `a << b` | 左移 `b` 位（低位补 0）      |
| `a >> b` | 右移 `b` 位（逻辑右移，高位补 0） |

#### 4. 逻辑运算符（短路与非短路）

| 运算符      | 操作数    | 结果     | 语义                                             |
| -------- | ------ | ------ | ---------------------------------------------- |
| `a & b`  | `bool` | `bool` | 逻辑与（短路）：若 `a` 为 `false` 则直接返回 `false`，不计算 `b`。 |
| `a \| b` | `bool` | `bool` | 逻辑或（短路）：若 `a` 为 `true` 则直接返回 `true`，不计算 `b`。   |
| `a && b` | `bool` | `bool` | 非短路逻辑与：同时计算 `a` 和 `b`，返回两者逻辑与。                 |
| `a \|    | b`     | `bool` | `bool`                                         |

> 非短路版本 `&&` 和 `||` 在操作数为布尔时优先于位运算，且不会短路。

#### 5. 集合运算符（仅适用于 `set` / `undset`）

| 运算符      | 语义                      | 示例                           |
| -------- | ----------------------- | ---------------------------- |
| `a & b`  | 交集（返回新集合）               | `{1,2} & {2,3}` → `{2}`      |
| `a \| b` | 并集（返回新集合）               | `{1,2} \| {2,3}` → `{1,2,3}` |
| `a ^ b`  | 对称差集（仅在其中一个集合中的元素）      | `{1,2} ^ {2,3}` → `{1,3}`    |
| `a - b`  | 差集（在 `a` 中但不在 `b` 中的元素） | `{1,2} - {2,3}` → `{1}`      |

#### 6. 比较运算符（所有可比较类型）

| 运算符      | 适用类型           | 语义                                 |
| -------- | -------------- | ---------------------------------- |
| `a == b` | 任何类型           | 相等：值相同（对于容器，逐元素比较；对于 `list`，顺序敏感）。 |
| `a != b` | 任何类型           | 不等：与 `==` 相反。                      |
| `a < b`  | 数值、字符、字符串（字典序） | 小于。                                |
| `a > b`  | 同上             | 大于。                                |
| `a <= b` | 同上             | 小于等于。                              |
| `a >= b` | 同上             | 大于等于。                              |

#### 7. 三目条件运算符

`condition ? expr1 : expr2`

- `condition` 必须为 `bool` 类型。
- 若 `condition` 为 `true`，结果为 `expr1` 的值；否则为 `expr2` 的值。
- `expr1` 和 `expr2` 的类型应兼容（或可隐式转换），否则会引发编译错误。

#### 8. 赋值与复合赋值

| 运算符       | 语义                           |
| --------- | ---------------------------- |
| `a = b`   | 将 `b` 的值赋给 `a`（`a` 必须为左值）。   |
| `a += b`  | 等价于 `a = a + b`（但只计算一次 `a`）。 |
| `a -= b`  | `a = a - b`                  |
| `a *= b`  | `a = a * b`                  |
| `a /= b`  | `a = a / b`                  |
| `a //= b` | `a = a // b`                 |
| `a %= b`  | `a = a % b`                  |
| `a **= b` | `a = a ** b`                 |
| `a &= b`  | `a = a & b`（整数位与，或集合交集）      |
| `a \|= b` | `a = a \| b`（整数位或，或集合并集）     |
| `a ^= b`  | `a = a ^ b`（整数异或，或集合对称差）     |
| `a <<= b` | `a = a << b`（仅整数）            |
| `a >>= b` | `a = a >> b`（仅整数）            |

> 复合赋值中的 `&=`、`|=`、`^=` 会根据 `a` 的类型自动选择位运算或集合运算。

---

### 运算符与 `memadr` 的整合

- `@x` 取变量地址，返回 `memadr`。

- `~adr` 解引用 `memadr`，可读取或修改指向的内存。

- 对于整数，`~` 仍作为按位取反，与 `~adr` 通过操作数类型区分，不产生歧义。

- 示例：
  
  ```
  int a = 10;                 # 声明整数
  memadr p = @a;              # 取地址
  ~p = 20;                    # 修改 a 的值
  int b = ~p;                 # 读取 a 的值，b = 20
  int c = ~0b1010;            # 按位取反，c 为二进制 ...0101
  ```

---

### 使用示例（含注释）

```
# 算术与幂
int x = 2 + 3 * 4;             # 14（* 优先）
double y = 2 ** 10;            # 1024.0

# 字符串与列表拼接
str s = "Hello" + " " + "World";   # "Hello World"
list l = [1, 2] + [3, 4];          # [1, 2, 3, 4]
list r = [0] * 5;                  # [0, 0, 0, 0, 0]

# 集合运算
set a = {1, 2, 3};
set b = {3, 4, 5};
set inter = a & b;                 # {3}
set union = a | b;                 # {1,2,3,4,5}
set diff = a - b;                  # {1,2}
set sym = a ^ b;                   # {1,2,4,5}

# 比较与三目
bool ok = (x > 10) ? true : false;

# 逻辑与短路
if (a != None && a.len() > 0) { ... }   # 短路保护，不会在 a 为 None 时访问 len

# 位运算
int flags = 0b1100;
int masked = flags & 0b1010;       # 0b1000

# 取地址与解引用
int val = 42;
memadr addr = @val;
~addr = 100;                       # val 变为 100
print(~addr);                      # 输出 100
```

---

### 注意事项

- **类型提升**：算术运算中，若操作数类型不同，会进行隐式提升（如 `int` + `float` → `float`）。
- **集合运算**仅对 `set` 和 `undset` 有效，对其他类型使用 `&`、`|`、`^`、`-` 会按位运算或数值运算处理。
- **字符串/列表重复**：`*` 右侧必须为无符号整数，否则编译报错。
- **短路逻辑** `&&` 和 `||` 仅用于布尔表达式，且不重载。
- **非短路逻辑** `&` 和 `|` 在布尔上下文中仍会计算两个操作数，适合用于需要副作用的情况（但应谨慎）。
- **赋值运算符**的左侧必须为可修改的左值（变量或可写内存地址）。

---

## 1. 数值类型

### 1.1 整数类型

| 类型名      | 全称                     | 范围                     | 说明          |
| -------- | ---------------------- | ---------------------- | ----------- |
| `int`    | integer                | $-2^{31}$ ~ $2^{31}-1$ | 标准有符号32位整数。 |
| `uint`   | unsigned integer       | $0$ ~ $2^{32}-1$       | 标准无符号32位整数。 |
| `short`  | short integer          | $-2^{15}$ ~ $2^{15}-1$ | 有符号16位整数。   |
| `ushort` | unsigned short integer | $0$ ~ $2^{16}-1$       | 无符号16位整数。   |
| `long`   | long integer           | $-2^{63}$ ~ $2^{63}-1$ | 有符号64位整数。   |
| `ulong`  | unsigned long integer  | $0$ ~ $2^{64}-1$       | 无符号64位整数。   |

### 1.2 浮点类型

| 类型名      | 全称                 | 标准       | 大约范围                                        | 说明           |
| -------- | ------------------ | -------- | ------------------------------------------- | ------------ |
| `float`  | (single-precision) | IEEE-754 | $2^{-126}$ ~ $(2-2^{-23}) \times 2^{127}$   | 单精度浮点数。      |
| `double` | (double-precision) | IEEE-754 | $2^{-1022}$ ~ $(2-2^{-52}) \times 2^{1023}$ | 双精度浮点数，精度更高。 |

### 1.3 字符与地址类型

| 类型名       | 全称                | 表示范围 / 说明                          | 存储空间    |
| --------- | ----------------- | ---------------------------------- | ------- |
| `char`    | ASCII character   | 0~255（ASCII表）                      | 8 bits  |
| `unichar` | Unicode character | `(0x00)` ~ `(0x10FFFF)`（Unicode码点） | 32 bits |
| `memadr`  | memory address    | 存储任意变量的内存地址                        | 平台相关    |

#### `memadr` 操作详解

`memadr` 提供底层内存操作能力（**请谨慎使用**）。

| 操作      | 语法                | 说明                              |
| ------- | ----------------- | ------------------------------- |
| 获取地址    | `memadr adr = a;` | 将变量 `a` 的地址存储到 `adr` 中。         |
| 修改指向    | `adr = b;`        | 使 `adr` 指向变量 `b` 的地址。           |
| 删除指向的内存 | `del ~adr;`       | **危险**：释放 `adr` 指向的内存，可能导致悬挂指针。 |
| 删除地址变量  | `del adr;`        | 仅删除 `adr` 变量本身，不影响其指向的内存。       |
| 读取指向的值  | `print(~adr);`    | 读取 `adr` 指向地址中存储的值。             |
| 修改指向的值  | `~adr = 1000;`    | 修改 `adr` 指向地址中存储的值。             |

### 1.4 布尔类型

| 类型名    | 全称      | 取值               | 说明                  |
| ------ | ------- | ---------------- | ------------------- |
| `bool` | boolean | `true` / `false` | 逻辑布尔值，支持与（`&&`）、或（` |

---

## 2. 字符串与二进制类型

### 2.1 `str` (ASCII 字符串)

- 元素为 **ASCII** 字符 (0~255)。
- 长度无限（受内存限制）。
- 支持 `[]` 运算符访问和修改任意位置的字符（索引从 0 开始）。

| 方法        | 参数                                     | 返回值     | 说明                            |
| --------- | -------------------------------------- | ------- | ----------------------------- |
| `append`  | `(str string, ulong pos = self.len())` | `None`  | 在 `pos` 位置插入 `string`。        |
| `append`  | `(char c, ulong count, ulong pos)`     | `None`  | 在 `pos` 位置插入 `count` 个字符 `c`。 |
| `erase`   | `(ulong left, ulong right)`            | `None`  | 删除区间 `[left, right]` 内的字符。    |
| `len`     | `()`                                   | `ulong` | 返回字符串长度。                      |
| `find`    | `(str string)`                         | `long`  | 返回子串首次出现的起始索引；若未找到，返回 `-1`。   |
| `count`   | `(str s)`                              | `ulong` | 返回子串 `s` 出现的次数。               |
| `count`   | `(char c)`                             | `ulong` | 返回字符 `c` 出现的次数。               |
| `replace` | `(str a, str b)`                       | `None`  | 将所有出现的子串 `a` 替换为 `b`。         |
| `replace` | `(ulong l, ulong r, str string)`       | `None`  | 将区间 `[l, r]` 替换为 `string`。    |
| `slice`   | `(ulong start, ulong end)`             | `str`   | 返回子串 `[start, end)`。          |
| `to_list` | `()`                                   | `list`  | 返回由每个字符组成的列表。                 |

### 2.2 `unistr` (Unicode 字符串)

- 元素为 **Unicode** 字符 (码点 `0x00` ~ `0x10FFFF`)。
- 长度无限（受内存限制）。
- 支持 `[]` 运算符访问和修改任意位置的字符（索引从 0 开始）。

| 方法        | 参数                                        | 返回值      | 说明                            |
| --------- | ----------------------------------------- | -------- | ----------------------------- |
| `append`  | `(unistr string, ulong pos = self.len())` | `None`   | 在 `pos` 位置插入 `string`。        |
| `append`  | `(unichar c, ulong count, ulong pos)`     | `None`   | 在 `pos` 位置插入 `count` 个字符 `c`。 |
| `erase`   | `(ulong left, ulong right)`               | `None`   | 删除区间 `[left, right]` 内的字符。    |
| `len`     | `()`                                      | `ulong`  | 返回字符串长度。                      |
| `find`    | `(unistr string)`                         | `long`   | 返回子串首次出现的起始索引；若未找到，返回 `-1`。   |
| `count`   | `(unistr s)`                              | `ulong`  | 返回子串 `s` 出现的次数。               |
| `count`   | `(unichar c)`                             | `ulong`  | 返回字符 `c` 出现的次数。               |
| `replace` | `(unistr a, unistr b)`                    | `None`   | 将所有出现的子串 `a` 替换为 `b`。         |
| `replace` | `(ulong l, ulong r, unistr string)`       | `None`   | 将区间 `[l, r]` 替换为 `string`。    |
| `slice`   | `(ulong start, ulong end)`                | `unistr` | 返回子串 `[start, end)`。          |
| `to_list` | `()`                                      | `list`   | 返回由每个字符组成的列表。                 |

### 2.3 `bin` (二进制字节序列)

`bin` 表示一个原始的字节序列，每个字节为无符号8位整数（0~255）。它支持按字节索引访问、切片、拼接及多种检索操作。

#### 声明与初始化

```
bin b;                              # 声明一个空二进制对象
bin b = [0x00, 0xFF, 0x7F];         # 以字节列表初始化
bin b = "hello";                    # 从ASCII字符串初始化（按字节编码）
bin b = {0x48, 0x65};               # 另一种初始化方式（花括号）
```

#### 方法列表

| 方法        | 参数                                     | 返回值      | 说明                              |
| --------- | -------------------------------------- | -------- | ------------------------------- |
| `append`  | `(ulong byte, ulong pos = self.len())` | `None`   | 在 `pos` 位置插入一个字节（0~255）。        |
| `append`  | `(bin bytes, ulong pos = self.len())`  | `None`   | 在 `pos` 位置插入另一个二进制对象。           |
| `erase`   | `(ulong left, ulong right)`            | `None`   | 删除区间 `[left, right]` 内的字节。      |
| `len`     | `()`                                   | `ulong`  | 返回字节数。                          |
| `find`    | `(bin pattern)`                        | `long`   | 返回模式首次出现的起始索引；未找到返回 `-1`。       |
| `count`   | `(bin pattern)`                        | `ulong`  | 返回模式出现的次数。                      |
| `replace` | `(bin old, bin new)`                   | `None`   | 将所有出现的 `old` 替换为 `new`。         |
| `replace` | `(ulong l, ulong r, bin bytes)`        | `None`   | 将区间 `[l, r]` 替换为 `bytes`。       |
| `slice`   | `(ulong start, ulong end)`             | `bin`    | 返回子区间 `[start, end)` 的副本。       |
| `hex`     | `()`                                   | `str`    | 返回十六进制字符串表示（如 `"FFA0"`）。        |
| `decode`  | `(str encoding = "utf-8")`             | `unistr` | 按指定编码解码为 Unicode 字符串（默认 UTF-8）。 |
| `encode`  | `(unistr s, str encoding = "utf-8")`   | `bin`    | 将 Unicode 字符串编码为二进制（静态方法）。      |

---

## 3. 列表类型 (`list`)

`list` 是一个通用的、可动态增长的**双向链表**，可存储任意类型的元素。

### 定义与初始化

```
list a;             # 声明一个空列表
list a = [1, 5];    # 声明并初始化为包含元素 1 和 5 的列表
```

### 方法列表（含新增常用方法）

| 方法         | 参数                                                                                       | 返回值      | 说明                                                                   |
| ---------- | ---------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------- |
| `append`   | `(object t, ulong pos = self.end())`                                                     | `None`   | 在 `pos` 位置插入元素 `t`（默认尾部）。                                            |
| `append`   | `(object t, ulong count, ulong pos)`                                                     | `None`   | 在 `pos` 位置插入 `count` 个元素 `t`。                                        |
| `insert`   | `(ulong pos, object t)`                                                                  | `None`   | 在 `pos` 位置插入一个元素（等效于 `append(t, pos)`）。                              |
| `erase`    | `(ulong left, ulong right)`                                                              | `None`   | 删除区间 `[left, right]` 内的所有元素。                                         |
| `remove`   | `(object t)`                                                                             | `bool`   | 删除第一个等于 `t` 的元素，成功返回 `true`，否则返回 `false`。                            |
| `len`      | `()`                                                                                     | `ulong`  | 返回列表的元素个数。                                                           |
| `begin`    | `()`                                                                                     | `ulong`  | 返回起始下标（固定为 `0`）。                                                     |
| `end`      | `()`                                                                                     | `ulong`  | 返回尾部位置（即元素个数）。                                                       |
| `clear`    | `()`                                                                                     | `None`   | 清空列表中的所有元素。                                                          |
| `contains` | `(object t)`                                                                             | `bool`   | 若列表包含 `t` 返回 `true`，否则 `false`。                                      |
| `find`     | `(object t)`                                                                             | `long`   | 返回元素 `t` 首次出现的索引；若未找到，返回 `-1`。                                       |
| `count`    | `(object s)`                                                                             | `ulong`  | 返回元素 `s` 在列表中出现的次数。                                                  |
| `replace`  | `(object a, object b)`                                                                   | `None`   | 将所有等于 `a` 的元素替换为 `b`。                                                |
| `replace`  | `(ulong l, ulong r, list lst)`                                                           | `None`   | 将区间 `[l, r]` 替换为列表 `lst`。                                            |
| `reverse`  | `()`                                                                                     | `None`   | 反转列表的元素顺序。                                                           |
| `slice`    | `(ulong start, ulong end)`                                                               | `list`   | 返回子列表 `[start, end)` 的副本。                                            |
| `sort`     | `(ulong l = self.begin(), ulong r = self.end(), function cmp = (a, b) => a < b ? 1 : 0)` | `None`   | 对区间 `[l, r]` 按比较函数 `cmp` 排序。`cmp` 返回 `1` 表示 `a` 应排在 `b` 之前，否则保持相对顺序。 |
| `shuffle`  | `(ulong l = self.begin(), ulong r = self.end(), seed = __SEED)`                          | `None`   | 使用种子 `seed` 打乱区间 `[l, r]` 内的元素。                                      |
| `sum`      | `(ulong l = self.begin(), ulong r = self.end())`                                         | `long`   | 计算区间 `[l, r]` 内所有元素的和（元素需支持加法运算）。                                    |
| `mean`     | `(ulong l = self.begin(), ulong r = self.end(), ulong decimal-precision = 15)`           | `double` | 计算区间 `[l, r]` 内元素的平均值，结果保留 `decimal-precision` 位小数。                  |
| `copy`     | `()`                                                                                     | `list`   | 返回列表的浅拷贝（元素引用相同）。                                                    |

---

## 4. 容器类型

以下容器类型均支持存储任意类型的元素（但 `set`、`undset`、`dict` 对键类型有特定要求）。所有容器均已内置 `clear()` 方法。

### 4.1 栈 (`stack`)

`stack` 是一个后进先出（LIFO）的容器，基于动态数组或链表实现，大小无上限。

#### 声明与初始化

```
stack s;                # 声明一个空栈
stack s = [1, 2, 3];    # 声明并初始化为包含 1,2,3 的栈（栈顶为最后一个元素）
```

#### 方法列表

| 方法         | 参数           | 返回值      | 说明                                      |
| ---------- | ------------ | -------- | --------------------------------------- |
| `push`     | `(object t)` | `None`   | 将元素 `t` 压入栈顶。                           |
| `pop`      | `()`         | `object` | 弹出栈顶元素并返回该元素（若栈为空则抛出异常）。                |
| `top`      | `()`         | `object` | 返回栈顶元素的引用（若栈为空则抛出异常）。                   |
| `empty`    | `()`         | `bool`   | 若栈为空返回 `true`，否则返回 `false`。             |
| `len`      | `()`         | `ulong`  | 返回栈中元素个数。                               |
| `clear`    | `()`         | `None`   | 清空栈中所有元素。                               |
| `contains` | `(object t)` | `bool`   | 若栈中包含元素 `t` 返回 `true`，否则 `false`（顺序搜索）。 |
| `copy`     | `()`         | `stack`  | 返回栈的浅拷贝。                                |

---

### 4.2 队列 (`queue`)

`queue` 是一个先进先出（FIFO）的容器，基于动态数组或链表实现，大小无上限。

#### 声明与初始化

```
queue q;                # 声明一个空队列
queue q = [1, 2, 3];    # 声明并初始化为包含 1,2,3 的队列（队首为第一个元素）
```

#### 方法列表

| 方法         | 参数           | 返回值      | 说明                                       |
| ---------- | ------------ | -------- | ---------------------------------------- |
| `push`     | `(object t)` | `None`   | 将元素 `t` 入队（添加到队尾）。                       |
| `pop`      | `()`         | `object` | 弹出队首元素并返回该元素（若队列为空则抛出异常）。                |
| `front`    | `()`         | `object` | 返回队首元素的引用（若队列为空则抛出异常）。                   |
| `back`     | `()`         | `object` | 返回队尾元素的引用（若队列为空则抛出异常）。                   |
| `empty`    | `()`         | `bool`   | 若队列为空返回 `true`，否则返回 `false`。             |
| `len`      | `()`         | `ulong`  | 返回队列中元素个数。                               |
| `clear`    | `()`         | `None`   | 清空队列中所有元素。                               |
| `contains` | `(object t)` | `bool`   | 若队列中包含元素 `t` 返回 `true`，否则 `false`（顺序搜索）。 |
| `copy`     | `()`         | `queue`  | 返回队列的浅拷贝。                                |

---

### 4.3 有序集合 (`set`)

`set` 是一个有序容器，元素唯一且自动按升序排列（基于 `<` 比较）。元素类型必须支持比较运算符。

#### 声明与初始化

```
set s;                  # 声明一个空集合
set s = [3, 1, 2];      # 声明并初始化为 {1, 2, 3}（自动排序去重）
```

#### 方法列表（含集合运算）

| 方法                     | 参数            | 返回值      | 说明                                           |
| ---------------------- | ------------- | -------- | -------------------------------------------- |
| `insert`               | `(object t)`  | `bool`   | 插入元素 `t`，若已存在则不操作并返回 `false`，否则插入并返回 `true`。 |
| `erase`                | `(object t)`  | `bool`   | 删除元素 `t`，若存在则删除并返回 `true`，否则返回 `false`。      |
| `contains`             | `(object t)`  | `bool`   | 若元素 `t` 存在返回 `true`，否则返回 `false`。            |
| `empty`                | `()`          | `bool`   | 若集合为空返回 `true`，否则返回 `false`。                 |
| `len`                  | `()`          | `ulong`  | 返回集合中元素个数。                                   |
| `clear`                | `()`          | `None`   | 清空集合中所有元素。                                   |
| `min`                  | `()`          | `object` | 返回最小元素（若为空则抛出异常）。                            |
| `max`                  | `()`          | `object` | 返回最大元素（若为空则抛出异常）。                            |
| `lower_bound`          | `(object t)`  | `object` | 返回第一个不小于 `t` 的元素（若不存在则抛出异常）。                 |
| `upper_bound`          | `(object t)`  | `object` | 返回第一个大于 `t` 的元素（若不存在则抛出异常）。                  |
| `count`                | `(object t)`  | `ulong`  | 返回元素 `t` 的个数（因为集合唯一，结果总是 0 或 1）。             |
| `union`                | `(set other)` | `set`    | 返回当前集合与 `other` 的并集。                         |
| `intersection`         | `(set other)` | `set`    | 返回当前集合与 `other` 的交集。                         |
| `difference`           | `(set other)` | `set`    | 返回当前集合与 `other` 的差集（当前中有而 other 中没有）。        |
| `symmetric_difference` | `(set other)` | `set`    | 返回对称差集（仅在其中一个集合中出现的元素）。                      |
| `is_subset`            | `(set other)` | `bool`   | 若当前集合是 `other` 的子集返回 `true`。                 |
| `is_superset`          | `(set other)` | `bool`   | 若当前集合是 `other` 的超集返回 `true`。                 |
| `is_disjoint`          | `(set other)` | `bool`   | 若两个集合无共同元素返回 `true`。                         |
| `copy`                 | `()`          | `set`    | 返回集合的副本。                                     |

---

### 4.4 无序集合 (`undset`)

`undset` 即无序集合（哈希集合），元素唯一但不保证顺序。元素类型必须支持哈希计算和相等比较。

#### 声明与初始化

```
undset us;              # 声明一个空无序集合
undset us = [3, 1, 2];  # 声明并初始化为包含 3,1,2 的集合（顺序不定）
```

#### 方法列表（与 `set` 类似，但不保证顺序）

| 方法                     | 参数                         | 返回值      | 说明                                           |
| ---------------------- | -------------------------- | -------- | -------------------------------------------- |
| `insert`               | `(object t)`               | `bool`   | 插入元素 `t`，若已存在则不操作并返回 `false`，否则插入并返回 `true`。 |
| `erase`                | `(object t)`               | `bool`   | 删除元素 `t`，若存在则删除并返回 `true`，否则返回 `false`。      |
| `contains`             | `(object t)`               | `bool`   | 若元素 `t` 存在返回 `true`，否则返回 `false`。            |
| `empty`                | `()`                       | `bool`   | 若集合为空返回 `true`，否则返回 `false`。                 |
| `len`                  | `()`                       | `ulong`  | 返回集合中元素个数。                                   |
| `clear`                | `()`                       | `None`   | 清空集合中所有元素。                                   |
| `rehash`               | `(ulong new_bucket_count)` | `None`   | 调整哈希表桶数，用于优化性能。                              |
| `union`                | `(undset other)`           | `undset` | 返回并集。                                        |
| `intersection`         | `(undset other)`           | `undset` | 返回交集。                                        |
| `difference`           | `(undset other)`           | `undset` | 返回差集。                                        |
| `symmetric_difference` | `(undset other)`           | `undset` | 返回对称差集。                                      |
| `is_subset`            | `(undset other)`           | `bool`   | 子集判断。                                        |
| `is_superset`          | `(undset other)`           | `bool`   | 超集判断。                                        |
| `is_disjoint`          | `(undset other)`           | `bool`   | 不相交判断。                                       |
| `copy`                 | `()`                       | `undset` | 返回副本。                                        |

---

### 4.5 字典 (`dict`)

`dict` 是一种键值对映射容器，键唯一且无序（基于哈希表实现）。键类型必须支持哈希和相等比较。

#### 声明与初始化

```
dict d;                              # 声明一个空字典
dict d = { "name": "Alice", "age": 30 };   # 键值对初始化
dict d = [("key1", 1), ("key2", 2)];       # 从元组列表初始化
```

#### 方法列表（含常用操作）

| 方法           | 参数                                    | 返回值                | 说明                                        |
| ------------ | ------------------------------------- | ------------------ | ----------------------------------------- |
| `put`        | `(object key, object value)`          | `None`             | 插入或更新键 `key` 对应的值为 `value`。               |
| `get`        | `(object key)`                        | `object`           | 返回键 `key` 对应的值；若不存在则抛出异常。                 |
| `get`        | `(object key, object default)`        | `object`           | 返回键 `key` 对应的值；若不存在则返回 `default`。         |
| `has`        | `(object key)`                        | `bool`             | 若键 `key` 存在返回 `true`，否则 `false`。          |
| `erase`      | `(object key)`                        | `bool`             | 删除键 `key` 及其值，成功返回 `true`，否则 `false`。     |
| `pop`        | `(object key)`                        | `object`           | 删除键 `key` 并返回其值；若不存在则抛出异常。                |
| `pop`        | `(object key, object default)`        | `object`           | 删除键 `key` 并返回其值；若不存在则返回 `default`。        |
| `popitem`    | `()`                                  | `(object, object)` | 随机删除并返回一个键值对（若字典为空则抛出异常）。                 |
| `setdefault` | `(object key, object default = None)` | `object`           | 若键存在返回其值，否则插入键并设为 `default`，返回 `default`。 |
| `keys`       | `()`                                  | `list`             | 返回所有键组成的列表（顺序不定）。                         |
| `values`     | `()`                                  | `list`             | 返回所有值组成的列表（顺序与键一致）。                       |
| `items`      | `()`                                  | `list`             | 返回所有 `(key, value)` 元组组成的列表。              |
| `len`        | `()`                                  | `ulong`            | 返回键值对个数。                                  |
| `empty`      | `()`                                  | `bool`             | 若字典为空返回 `true`，否则 `false`。                |
| `clear`      | `()`                                  | `None`             | 清空所有键值对。                                  |
| `update`     | `(dict other)`                        | `None`             | 将另一个字典 `other` 的键值对合并进来（覆盖相同键）。           |
| `copy`       | `()`                                  | `dict`             | 返回字典的浅拷贝。                                 |
| `fromkeys`   | `(list keys, object default = None)`  | `dict`             | 静态方法，从键列表创建字典，所有值初始化为 `default`。          |

---

## 5. 复合类型

复合类型用于将多个值组合为一个结构。它们固定长度，且每个元素的类型在声明时指定。

### 5.1 对 (`pair`)

`pair` 是一个包含两个元素的复合类型，两个元素的类型可以不同，但需在声明时明确指定。

#### 声明与初始化

```
pair<type1, type2> p;              # 声明一个 pair，元素类型为 type1 和 type2
pair<int, str> p = {42, "hello"};  # 声明并初始化
pair<float, bool> p(3.14, true);   # 使用构造函数初始化
```

#### 访问元素

- 使用 `.first` 访问第一个元素。
- 使用 `.second` 访问第二个元素。

示例：

```
pair<int, str> p = {10, "world"};
int a = p.first;      # a = 10
str s = p.second;     # s = "world"
```

#### 方法

| 方法          | 参数                   | 返回值                  | 说明                      |
| ----------- | -------------------- | -------------------- | ----------------------- |
| `swap`      | `(pair other)`       | `None`               | 交换两个 pair 的内容（类型必须相同）。  |
| `make_pair` | `(type1 a, type2 b)` | `pair<type1, type2>` | 全局函数，用于创建 pair（类型自动推导）。 |

### 5.2 元组 (`tuple`)

`tuple` 是一个包含固定数量元素的复合类型，每个元素的类型可以不同，需在声明时指定完整的类型列表。

#### 声明与初始化

```
tuple<type1, type2, ...> t;                         # 声明空元组（可后续赋值）
tuple<int, str, float> t = {10, "hello", 3.14};    # 声明并初始化
tuple<int, double> t(5, 2.5);                      # 使用构造函数
```

#### 访问元素

- 使用模板化的 `get<index>(t)` 函数（索引从 0 开始）。
- 或者使用 `.get(index)` 方法（若支持）。

示例：

```
tuple<int, str, float> t = {42, "answer", 0.5};
int i = get<0>(t);      # i = 42
str s = get<1>(t);      # s = "answer"
float f = get<2>(t);    # f = 0.5
```

#### 方法

| 方法           | 参数               | 返回值          | 说明                           |
| ------------ | ---------------- | ------------ | ---------------------------- |
| `len`        | `()`             | `ulong`      | 返回元组中元素的个数（编译期常量）。           |
| `get`        | `(ulong index)`  | `object`     | 返回第 `index` 个元素的引用（运行期检查越界）。 |
| `swap`       | `(tuple other)`  | `None`       | 交换两个 tuple 的内容（类型必须完全一致）。    |
| `make_tuple` | `(a, b, c, ...)` | `tuple<...>` | 全局函数，根据参数自动推导类型创建元组。         |

---

## 使用说明

- **常量**：使用 `const` 声明的变量必须在声明时初始化，且之后不可修改。
- **字面量**：整数字面量可使用二进制（`0b`）、八进制（`0o`）、十六进制（`0x`）前缀；浮点数也支持十六进制表示。
- **复合类型**：`pair` 和 `tuple` 的元素类型在声明时必须完整指定，不支持类型推导（除非使用 `make_pair`/`make_tuple` 辅助函数）。
- **容器类型**（`list`、`stack`、`queue`、`set`、`undset`、`dict`）均不提供 `[]` 运算符进行随机访问（除 `str`、`unistr`、`bin` 外）。
- 对于 `set` 和 `undset`，元素类型必须满足相应的条件（可比较或可哈希），否则编译或运行时会报错。
- 对于 `dict`，键类型必须支持哈希和相等比较。
- 所有带有 `pos` 参数的方法，其默认值均为容器的尾部位置（即 `len()` 或 `end()`），确保插入操作默认在末尾进行。
- **注释**：所有代码示例中的注释均使用 `#` 符号，符合本语言规范。
  根据您的要求，我们**更新了“语言语法规范”章节**，采用新的函数定义方式（`def` + 自动返回值推导），修改了 `lambda` 语法，并**新增“标准函数库”子章节**，涵盖 `print`、`input`、类型转换、`format` 等常用内建函数。以下为完整的更新内容，您可以直接替换原“语言语法规范”章节，或将其整合到文档中。

---

## 语言语法规范

### 1. 词法规范

- **注释**：`#` 开头，直至行尾（支持行尾注释）。
- **标识符**：字母、数字、下划线，不能以数字开头，区分大小写。
- **关键字**：  
- `def`, `const`, `del`, `if`, `else`, `elif`, `for`, `while`, `break`, `continue`, `return`, `lambda`, `import`, `from`, `as`, `true`, `false`, `None`, `and`, `or`, `not`, `in`, `is`, `global`
- **字面量**：整数（十进制、二进制 `0b`、八进制 `0o`、十六进制 `0x`）、浮点数（含十六进制 `0x1.2p3`）、字符（`'A'`）、字符串（`"hello"`）、字节序列（`bin`）、布尔（`true`/`false`）、空值（`None`）。

---

### 2. 表达式

详见“运算符”章节，支持算术、比较、逻辑、位运算、集合运算、三目 `? :`、取地址 `@`、解引用 `~`

---

### 3. 声明与定义

#### 变量声明

```
typename var_name;                    # 默认初始化（数值为0，引用为None）
typename var_name = initial_value;    # 声明并初始化
typename a, b, c;                     # 同时声明多个
const typename CONST_NAME = value;    # 常量，必须初始化，不可修改
```

#### 函数定义

```
def func-name(type1 param1, type2 param2, ...) {
    # 函数体
    return result;   # 返回值类型由编译器自动推导
}
```

- **无需显式指定返回类型**，编译器根据 `return` 语句推导。
- 若无 `return` 语句，则返回 `None`。
- 参数类型**必须**标注（静态类型）。
- 支持默认参数：`def func(a: int, b: str = "default") { ... }`
- 支持可变参数：`def func(*args) { ... }`（`args` 为 `list`）

**示例**：

```
def add(x: int, y: int) {    # 自动推导返回类型为 int
    return x + y;
}
def greet(name: str) {       # 无返回，实际返回 None
    print("Hello, " + name);
}
```

#### Lambda

```
lambda(param1: type1, param2: type2, ...): {
    # 多行语句
    param1 += param2;
    return param1;
}
```

- 参数类型**可选**（若不写类型，则视为动态类型，由调用推断）。
- 函数体用花括号 `{}` 括起，可以包含多条语句。
- 必须包含 `return`（除非无返回值，则返回 `None`）。
- 常用于高阶函数（如 `sort` 的比较器）。

**示例**：

```
list.sort(cmp = lambda (a: int, b: int): {
    return a < b ? 1 : 0;
});
```

---

### 4. 语句

#### 赋值与复合赋值

```
var = expr;
var += expr;   # 对应运算符重载
```

#### 删除语句

```
del var;
del var1, var2;
del *;         # 删除所有变量（危险）
del ~ptr;      # 释放指针指向的内存
```

#### 条件语句

```
if (cond) {
    # ...
} elif (cond2) {
    # ...
} else {
    # ...
}
```

- `elif` 和 `else` 可选。

#### 循环

```
for (item in container) { ... }
for (i in range(0, 10)) { ... }   # range(start, end, step)
while (cond) { ... }
```

支持 `break`, `continue`。

#### 返回语句

```
return expr;   # 返回 expr
return;        # 返回 None
```

#### 异常处理

```
try { ... } catch (ExceptionType e) { ... } finally { ... }
```

#### 导入

```
import module;
from module import func, class;
import module as alias;
```

---

### 5. 标准函数库（Built-in Functions）

以下函数在所有模块中全局可用，无需导入。

| 函数           | 签名                                                   | 说明                                                                                  |
| ------------ | ---------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `print`      | `print(...)`                                         | 将参数转换为字符串并输出到标准输出，多个参数以空格分隔，自动换行。                                                   |
| `input`      | `input(prompt: str) -> str`                          | 输出提示符（可选），读取用户输入的一行字符串，返回 `str`。                                                    |
| `int`        | `int(x: any) -> int`                                 | 将 `x` 转换为 `int` 类型（若为字符串则按十进制解析；若为浮点则截断）。                                           |
| `long`       | `long(x: any) -> long`                               | 转换为 `long` 类型。                                                                      |
| `ulong`      | `ulong(x: any) -> ulong`                             | 转换为 `ulong` 类型。                                                                     |
| `float`      | `float(x: any) -> float`                             | 转换为 `float` 类型。                                                                     |
| `bool`       | `bool(x: any) -> bool`                               | 转换为 `bool` 类型（0、空串、空容器、`None` 为 `false`，其余为 `true`）。                                |
| `str`        | `str(x: any) -> str`                                 | 转换为字符串（ASCII 或 Unicode 视输入而定）。                                                      |
| `unistr`     | `unistr(x: any) -> unistr`                           | 转换为 Unicode 字符串。                                                                    |
| `bin`        | `bin(x: any) -> bin`                                 | 将整数、字节序列或字符串转换为 `bin` 类型。                                                           |
| `format`     | `format(value: any, format_spec: str) -> str`        | 按格式规范 `format_spec` 格式化 `value`，返回格式化后的字符串。<br>示例：`format(3.14, ".2f")` → `"3.14"`。 |
| `len`        | `len(container) -> ulong`                            | 返回容器（字符串、列表、字典、集合等）的长度。                                                             |
| `type`       | `type(obj) -> str`                                   | 返回对象类型的名称（如 `"int"`, `"str"`）。                                                      |
| `range`      | `range(start: int, end: int, step: int = 1) -> list` | 生成一个整数列表（惰性求值，实际为迭代器，但可遍历）。                                                         |
| `isinstance` | `isinstance(obj, typename) -> bool`                  | 检查对象是否为指定类型（或子类）。                                                                   |

**示例**：

```
# 输入输出
str name = input("Enter your name: ");
print("Hello,", name);

# 类型转换
int a = int("123");          # a = 123
float b = float("3.14");     # b = 3.14
long c = long(a);            # 提升为 long

# 格式化
str s = format(42, "X");     # "2A"（十六进制大写）
str pi = format(3.14159, ".2f");  # "3.14"

# 长度与类型
list lst = [1, 2, 3];
ulong n = len(lst);          # n = 3
str t = type(lst);           # "list"
```

---

### 6. 作用域与生命周期

- 变量作用域为声明所在的代码块（大括号内）。
- 函数内可访问外部变量，但修改外部变量需用 `global` 声明（默认只读）。
- 文件级别的变量为模块全局。

---

### 7. 完整示例

```
# 导入标准库（假设有 math）
import math;

# 常量
const PI = 3.14159;

# 函数定义（自动推导返回类型）
def circle_area(radius: double) {
    return PI * radius ** 2;
}

# 主程序
int a = 10;
double area = circle_area(a / 2.0);

# 条件
if (area > 50) {
    print("Area is large");
} else {
    print("Area is small");
}

# 列表与循环
list data = [1, 2, 3, 4, 5];
for (num in data) {
    print(num * 2);
}

# 使用 lambda 排序
data.sort(cmp = lambda (x: int, y: int): {
    return x > y ? 1 : 0;
});

# 字典操作
dict d = {"name": "Alice", "age": 30};
d.put("age", 31);
str name = d.get("name");

# 输入与转换
str user_input = input("Enter number: ");
int num = int(user_input);

# 内存操作
memadr ptr = @a;
~ptr = 100;        # a 变为 100

# 删除
del ptr;
```

---

### 8. 与类型系统的整合

- 所有变量、参数、返回值类型均遵循本文档“类型系统规范”中定义的类型（`int`, `str`, `list`, `set`, `dict` 等）。
- 运算符行为见“运算符”章节。
- 标准函数的行为与类型系统紧密配合（如类型转换函数会进行合理的截断、舍入或解析）。
